node_modules/
dist/
.astro/
.DS_Store
*.log
.venv/
__pycache__/

# secrets — never commit (repo is public)
.env
.env.*
!.env.example
secrets/
secrets/**
**/social.json
!**/social.example.json

# Observatory warehouse deploy stamp: written per host by deploy_warehouse.sh
# so a manifest built on vps-main can name the commit it came from. Host state,
# not source, and committing it would make one host's sha look like everyone's.
scripts/observatory/warehouse/PIPELINE_STAMP.json
